@model IEnumerable<DAL.Models.Extruder>

@{
    ViewData["Title"] = "Lista czytników";
}

<h1>Wytłaczarki</h1>

<p>
    <a asp-action="Create">Dodaj nowy</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |*@
                    @*@Html.ActionLink("Details", "Details", new { id = item.ReaderId }) |*@
                    @*<a class="btn btn-danger" asp-controller="Reader" asp-action="Delete" asp-route-id="@item.ReaderId" onclick="return confirm('Jesteś pewny?')">Usuń</a>*@
                    <a class="btn btn-primary" asp-controller="Extruder" asp-action="Edit" asp-route-id="@item.Id">Edytuj</a>
                    <a class="btn btn-info" asp-controller="Extruder" asp-action="Details" asp-route-id="@item.Id">Szczegóły</a>
                    <a class="btn btn-danger" asp-controller="Extruder" asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('Jesteś pewny?')">Usuń</a>

                </td>
            </tr>
        }
    </tbody>
</table>